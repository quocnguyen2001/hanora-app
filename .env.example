# Base URL của API.
#
# Dev: Vite chạy ở :5173, API ở :8080 → cần URL tuyệt đối.
# Production: FE và API dùng CHUNG ORIGIN, nginx phục vụ file tĩnh và proxy
# /api (quyết định D12) → để TRỐNG, request đi đường dẫn tương đối.
VITE_API_URL=http://localhost:8080
